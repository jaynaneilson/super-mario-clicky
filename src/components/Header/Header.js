import React from "react";
import "./Header.css";

const  Header = props => (
        <div className="header">
            <h1>Super Mario Clicky</h1>
            <h2>Click on an image but not on the same one twice!</h2>
        </div>

);
  
export default Header;